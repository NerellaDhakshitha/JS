let num=[1,2,3,4,5];
const newArr=num.map((val)=>
{                                           //map is used to create a new array from the return values of a array
    return val;
})

console.log(newArr);